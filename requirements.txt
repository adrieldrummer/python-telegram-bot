fastapi>=0.110
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1

# servidor ASGI — usado em VPS/Docker (na Vercel quem serve é a própria plataforma)
uvicorn>=0.27

# HTTP de saída: API de Conversões do Meta
httpx>=0.27

# PostgreSQL — usado quando DATABASE_URL está definido (Supabase, Neon, RDS).
# Em SQLite (desenvolvimento/VPS) esta dependência fica ociosa.
psycopg[binary]>=3.1
